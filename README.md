Visualizador de Trajetórias — Cartões de Narizes

Como usar

- Abra `index.html` no navegador.
-- No cartão individual, clique em "Abrir Trajetória (PDF)" — o PDF será aberto em uma nova aba (padrão).

Notas

- O visualizador usa PDF.js (CDN). Se o PDF estiver hospedado num bucket com CORS restrito, ajuste as regras ou torne o arquivo público.
-- Atalhos no visualizador (quando disponível): ← e → para navegar entre páginas; +/- para zoom.

Arquivos relevantes

-- `index.html` — interface principal e painel administrativo.

Quer que eu adicione suporte a abrir sempre em um visualizador embutido em vez do PDF bruto, ou deixe como está (abrir em nova aba)?